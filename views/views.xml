<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="intern_bridge.list">
      <field name="name">intern_bridge list</field>
      <field name="model">intern_bridge.intern_bridge</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="intern_bridge.action_window">
      <field name="name">intern_bridge window</field>
      <field name="res_model">intern_bridge.intern_bridge</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="intern_bridge.action_server">
      <field name="name">intern_bridge server</field>
      <field name="model_id" ref="model_intern_bridge_intern_bridge"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="intern_bridge" id="intern_bridge.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="intern_bridge.menu_1" parent="intern_bridge.menu_root"/>
    <menuitem name="Menu 2" id="intern_bridge.menu_2" parent="intern_bridge.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="intern_bridge.menu_1_list" parent="intern_bridge.menu_1"
              action="intern_bridge.action_window"/>
    <menuitem name="Server to list" id="intern_bridge" parent="intern_bridge.menu_2"
              action="intern_bridge.action_server"/>
-->
  </data>
</odoo>
